const socket = io();

socket.on('connect', () => {
    // const queryString = window.location.search;
    // const urlParams = new URLSearchParams(queryString);
    // console.log(`<div>${urlParams.get('userName')}`);
    // const userEnteredHTML = `<div>${urlParams.get('userName')} has joined the room</div>`;
    // document.getElementById('content').appendChild(userEnteredHTML);


});